Тесты по модулям добавляются здесь по мере готовности каждого модуля
(структура зеркалит `app/modules/`). Пример: `tests/modules/test_tasks_service.py`
появится вместе с реализацией `TaskService` на Этапе 7.
