"""У модуля `auth` нет собственной ORM-модели.

Единственная сущность-пользователь (email, hashed_password, ...) — одна
таблица `users`, которой владеет модуль `users` (см. app/modules/users/model.py
и docs/01-architecture-and-design.md, раздел 4 — ERD не разводит auth-данные
и профиль по разным таблицам).

`AuthService` (Этап 5) работает с `UserRepository` из `users`, а не заводит
дублирующую модель — то же правило межмодульных границ, что и везде в проекте
(см. раздел 3.1: "Модули не импортируют друг друга напрямую на уровне
репозиториев — только через интерфейсы сервисов").
"""
